# FerroTwin Azure Functions runtime dependencies (SERVING ONLY).
# Deliberately excludes torch/torchvision — inference runs on onnxruntime + NumPy,
# which keeps the deployment package small enough to publish to Azure Functions.
# Training / ONNX-export dependencies live in ml/requirements.txt.

azure-functions==1.21.3

# Azure SDKs
azure-core==1.30.2
azure-identity==1.17.1
azure-digitaltwins-core==1.2.0
azure-data-tables==12.5.0
azure-storage-blob==12.22.0
azure-eventgrid==4.20.0

# Inference + image handling
onnxruntime==1.18.1
numpy==1.26.4
pillow==10.4.0
PyYAML